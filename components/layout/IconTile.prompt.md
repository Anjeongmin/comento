Rounded-square icon container — the tile behind call-type and feature icons. Icon inherits `currentColor`.

```jsx
<IconTile><PhoneIcon/></IconTile>
<IconTile variant="brand" size={40}><MicIcon/></IconTile>
```
