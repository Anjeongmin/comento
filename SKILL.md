---
name: call-coach-design
description: Use this skill to generate well-branded interfaces and assets for the AI 통화 분석 (Call Coach) app — a Korean AI call-analysis & conversation-coaching product — either for production or throwaway prototypes/mocks. Contains essential design guidelines, colors, type, fonts, the tomato mascot, and UI-kit components for prototyping.
user-invocable: true
---

Read the `readme.md` file within this skill first, then explore the other files (`tokens/`, `components/`, `ui_kits/`, `assets/`, `guidelines/`).

The brand in one line: a vivid single-red (`#FF3639`) over white + cool grays, friendly rounded cards, polite Korean copy (Inter + Pretendard), and a cute 3D tomato-with-headset mascot. No emoji, no secondary color, no decorative gradients.

If creating visual artifacts (slides, mocks, throwaway prototypes), copy the assets out and produce static HTML files for the user to view — link `styles.css` for tokens and reuse the patterns in `ui_kits/call-coach/index.html`. If working on production code, copy the assets and read the rules here to design as an expert in this brand.

If the user invokes this skill without other guidance, ask them what they want to build, ask a few focused questions, then act as an expert designer who outputs HTML artifacts or production code as needed.
