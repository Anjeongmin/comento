Text field with a red caption label. `underline` matches the login screen; `filled` is a rounded gray field for general forms. Focus state turns brand red.

```jsx
<Input label="ID" placeholder="아이디를 입력하세요" />
<Input label="PASSWORD" type="password" variant="underline" />
<Input variant="filled" placeholder="검색" />
```
