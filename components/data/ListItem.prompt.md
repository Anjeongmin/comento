Call-history list row: leading icon tile, stacked title + meta, trailing action chip.

```jsx
<ListItem icon={<PhoneIncoming/>} title="이지수 팀장" meta="오전 11:30 · 05분 12초" action="요약 보기" divider />
```

Pass a string `action` for the default gray chip, or a node for custom trailing content.
